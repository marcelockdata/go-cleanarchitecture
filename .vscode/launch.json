{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Launch Package",
            "type": "go",
            "request": "launch",
            "mode": "debug",
            "program": "${workspaceFolder}/cmd", // Points to the cmd directory
            "cwd": "${workspaceFolder}"         // Sets the current working directory to the workspace root
        }
    ]
}